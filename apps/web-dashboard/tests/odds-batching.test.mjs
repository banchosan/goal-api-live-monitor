import test from 'node:test';import assert from 'node:assert/strict';
import {ODDS_BATCH_SIZE,splitOddsBatch,uniqueByFixtureId,planOddsResume} from '../lib/odds-batching.ts';
test('46 fixtures are safely split into 40 plus 6',()=>{const items=Array.from({length:46},(_,i)=>({fixtureId:`f${i}`})),first=splitOddsBatch(items);assert.equal(ODDS_BATCH_SIZE,40);assert.equal(first.batch.length,40);assert.equal(first.remaining.length,6);assert.equal(splitOddsBatch(first.remaining).batch.length,6)});
test('100 fixtures split without losing identity and duplicate fixture candidates are removed',()=>{const items=Array.from({length:100},(_,i)=>({fixtureId:`f${i}`}));assert.equal(splitOddsBatch(items).batch.length,40);assert.equal(uniqueByFixtureId([...items,{fixtureId:'f0'}]).length,100)});
test('saved form odds resume processes all when nothing is complete and skips only durable successes',()=>{const items=Array.from({length:46},(_,i)=>({fixtureId:`f${i}`}));const initial=planOddsResume(items,new Set(),x=>x.fixtureId);assert.equal(initial.batch.length,40);assert.equal(initial.pending.length,46);const resumed=planOddsResume(items,new Set(['f0','f1','f2']),x=>x.fixtureId);assert.equal(resumed.batch[0].fixtureId,'f3');assert.equal(resumed.pending.length,43);assert.equal(resumed.remaining.length,3)});
