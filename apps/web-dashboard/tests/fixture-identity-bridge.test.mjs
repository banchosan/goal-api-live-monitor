import test from 'node:test';import assert from 'node:assert/strict';import {matchFixturePair} from '../lib/fixture-identity-bridge.ts';
const g={id:'g',kickoffUtc:'2026-01-01T12:00:00Z',league:'Premier League',country:'England',home:{id:'gh',name:'Alpha FC'},away:{id:'ga',name:'Beta FC'}};
test('bridge requires multi-field fixture evidence',()=>{assert.equal(matchFixturePair(g,{...g,id:'a',home:{id:'ah',name:'Alpha FC'},away:{id:'aa',name:'Beta FC'}}).status,'SAFE_MATCH');assert.equal(matchFixturePair(g,{...g,id:'a',league:'Other',home:{id:'ah',name:'Alpha FC'},away:{id:'aa',name:'Beta FC'}}).status,'POSSIBLE_MATCH');assert.equal(matchFixturePair(g,{...g,id:'a',home:{id:'ah',name:'Alpha FC'},away:{id:'aa',name:'Else'}}).status,'NO_MATCH')});
test('bridge treats missing identity context as NO_MATCH without normalizing an empty value',()=>{const api={...g,id:'a',home:{id:'ah',name:'Alpha FC'},away:{id:'aa',name:'Beta FC'}};for(const broken of [{...g,home:{id:'gh',name:undefined}},{...g,away:{id:'ga',name:undefined}},{...g,league:undefined},{...g,kickoffUtc:null}]){const result=matchFixturePair(broken,api);assert.equal(result.status,'NO_MATCH');assert.match(result.reason,/insufficient_context|invalid kickoff/)}});
test('country is optional evidence and a valid fixture remains SAFE when it is absent',()=>{const api={...g,id:'a',country:undefined,home:{id:'ah',name:'Alpha FC'},away:{id:'aa',name:'Beta FC'}};assert.equal(matchFixturePair({...g,country:undefined},api).status,'SAFE_MATCH')});
